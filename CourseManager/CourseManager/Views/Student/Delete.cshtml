@model Student
@{
}

<h3>@Model.Name<h3 />

<p>Do you want to delete this student?</p>

@using (Html.BeginForm("Delete", "Student", FormMethod.Post))
{
    @Html.HiddenFor(s => s.Id);

    <button type="submit">Yes</button>
}

<br />
<a href="~/Student/Index">No</a>
