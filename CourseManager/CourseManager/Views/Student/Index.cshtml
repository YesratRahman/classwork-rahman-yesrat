@model IEnumerable<Student>
@{
}

<table>

    @foreach (Student s in Model)
    {
        <tr>
            <td>@Html.ActionLink(s.Name, "Details", new { id = s.Id })</td>

            <td>@Html.ActionLink("Edit", "Edit", new { id = s.Id })</td>
            <td>Delete</td>
        </tr>
    }


</table>
